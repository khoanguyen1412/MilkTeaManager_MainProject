<Window x:Class="MilkTeaManager.Views.SellProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MilkTeaManager.Views"
          xmlns:viewmodel="clr-namespace:MilkTeaManager.ViewModels"
        mc:Ignorable="d"
        Title="SellProductView" Height="450" Width="800">
    <Window.Resources>
        <viewmodel:SellProductViewModel x:Key="vm"></viewmodel:SellProductViewModel>
    </Window.Resources>
    <Grid DataContext="{Binding Source={StaticResource vm}}">
        <ComboBox HorizontalAlignment="Left" 
                  Margin="183,39,0,0" 
                  VerticalAlignment="Top" 
                  Width="120" 
                  ItemsSource="{Binding Path=LoaiSPs}"  
                  SelectedItem="{Binding Path=SPerson}"
                  DisplayMemberPath="TENLOAISP"/>
        <TextBox HorizontalAlignment="Left" Height="23" Margin="226,177,0,0" TextWrapping="Wrap" 
                 Text="{Binding Path=Persons}" VerticalAlignment="Top" Width="120"/>
    </Grid>
</Window>
